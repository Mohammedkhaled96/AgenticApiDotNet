# Copy to .env and fill in. .env is git-ignored - never commit real values.

# Required: MySQL root password used by both the database and the API
MYSQL_ROOT_PASSWORD=

# Optional overrides
MYSQL_DATABASE=agentic_db
MYSQL_PORT=3306
OLLAMA_PORT=11434
API_PORT=8080
AI_MODEL_ID=llama3.2:3b
ASPNETCORE_ENVIRONMENT=Development
